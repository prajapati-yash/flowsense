import FlowSenseActionsFinal from 0x9c23faae746705fe

// Comprehensive action discovery for AI agents
access(all) fun main(category: String?): [FlowSenseActionsFinal.ActionMetadata] {
    // Discover all available actions or filter by category
    return FlowSenseActionsFinal.discoverActions(category: category)
}